import {Browser} from 'kogi'

const apiKey = "<< Get access token >>"

const page = await Browser.start(apiKey, "local", "New Test")

await page.navigate("localhost:4200")

await page.clearSession()

await page.click("Enter your email", {x: 689, y: 395})

await page.type("Email Address","ajazbeig@gmail.com", {x: 508, y: 363})

await page.pressKey("tab", {repeat: 1})

await page.type("Password","Password", {x: 508, y: 458})

await page.click("login button par click karo")

await page.click("click on toggle button")
await page.click("click on toggle button")

await page.close()